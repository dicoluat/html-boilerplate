{% set seoData = seoData|default([]) %}{% set arrScripts = arrScripts|default([]) %}{% set arrStyles = arrStyles|default([]) %}{% set components = components|default([]) %}{% for item in components %}{% for script in item.scripts %}{% set arrScripts = arrScripts|merge([script]) %}{% endfor %}{% set arrScripts = arrScripts|merge(['assets/js/sections/'~item.type~'/'~item.type~'.js']) %}{% for style in item.styles %}{% set arrStyles = arrStyles|merge([style]) %}{% endfor %}{% set arrStyles = arrStyles|merge(['assets/css/sections/'~item.type~'/'~item.type~'.css']) %}{% endfor %}
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="description" content="">
  <meta name="viewport" content="width=device-width, initial-scale=1, user-scalable=no, viewport-fit=cover">
  <meta name="format-detection" content="telephone=no">
  <title>{{ seoData.title }}</title>
  {% include "./includes/styles.twig" %}
  {% block styles %}
  {% endblock %}
</head>
<body>
<main>
  {% for item in components %}
    {% set componentData = item.data %}
    <!-- {{ item.type }} -->
{% include "../sections/"~item.type~"/"~item.type~item.sub~".twig" %}
    <!-- /{{ item.type }} -->
  {% endfor %}
</main>
{% include "./includes/scripts.twig" %}
{% block scripts %}{% endblock %}
</body>
</html>
